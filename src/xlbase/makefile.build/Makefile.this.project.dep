# output module name
OUTPUT_NAME = libxlbase.so

# target path
TARGET = $(XLCOMM_ROOT_PATH)/bin/$(XL_CROSS_PLATFORM)/$(OUTPUT_NAME)

# additional include dir
HEADER = -I./ \

# project dir list
DIR := . \
	$(XLCOMM_ROOT_PATH)/src/xlbase/thread \
